from os.path import realpath, dirname, split

PROJECT_ROOT_DIR = dirname(realpath(__file__)) # This is Project Root
LOG_DIR = str(PROJECT_ROOT_DIR) + "/logs/"
RESOURCE_DIR = str(PROJECT_ROOT_DIR) + "/resources/"
